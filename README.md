# wordpress-advanced-dorks
A collection of advanced Google dorks specifically for WordPress sites to find sensitive information (configuration files, database dumps, debug logs, exposed backups, API keys, etc.) — for authorized security testing only.
